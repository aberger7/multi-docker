sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t anberger/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true anberger/react-test npm test

after_success:
  - docker build -t anberger/multi-client ./client
  - docker build -t anberger/multi-nginx ./nginx
  - docker build -t anberger/multi-server ./server
  - docker build -t anberger/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push anberger/multi-client
  - docker push anberger/multi-nginx
  - docker push anberger/multi-server
  - docker push anberger/multi-worker
